# Raqeem Portfolio

React + Vite + TypeScript + Tailwind CSS portfolio.

## Run in VS Code

```bash
npm install
npm run dev
```

Then open the local URL shown by Vite, usually http://localhost:5173.

## Build

```bash
npm run build
npm run preview
```

The CV is available at `/Raqeem-CV.pdf`.
